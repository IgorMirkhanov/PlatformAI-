-- Core platform schema bootstrap marker
-- Full ORM tables are created by: python scripts/bootstrap_database.py
-- This migration records the baseline revision in schema_migrations tracking.

SELECT 1;
