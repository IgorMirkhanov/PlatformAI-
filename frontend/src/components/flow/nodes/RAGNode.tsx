"use client";

/**
 * RAGNode — alias of KnowledgeSearchNode for product naming.
 * Canvas type: ``rag`` | ``knowledgeSearch`` → backend ``knowledge_search``.
 */
export { KnowledgeSearchNode as RAGNode } from "./KnowledgeSearchNode";
